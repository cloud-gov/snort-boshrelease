<% p("snort.rules", []).each do |rule| %>
<%= rule %>
<% end %>

<% if_link("snort-config") do |config| %>
  <% config.p("snort.rules", []).each do |rule| %>
<%= rule %>
  <% end %>
<% end %>

<%
  if_link("curator") { |curator_link| suppress gen_id 1, sig_id 343080004, track by_src, ip curator_link.instances.first.address }
%>